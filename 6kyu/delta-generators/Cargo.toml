[package]
name = "delta-generators"
edition = "2021"

[dependencies]
heapless = "0.8.0"

[dev-dependencies]
rand = { version = "0.8.5", default-features = false, features = ["std"] }
rand_pcg = "0.3.1"

[lints]
workspace = true
