[package]
name = "generating-numbers-from-digits-number-1"
edition = "2021"

[dependencies]
num-bigint = "0.4.4"

[dev-dependencies]
rand = { version = "0.8.5", default-features = false, features = ["std"] }
rand_pcg = "0.3.1"

[lints]
workspace = true
