[package]
name = "digital"
edition = "2021"

[dependencies]
heapless = { workspace = true, optional = true }

[dev-dependencies]
criterion = { workspace = true }
num-traits = { workspace = true }

[features]
default = ["heapless"]

[lints]
workspace = true

[[bench]]
name = "bench"
harness = false
