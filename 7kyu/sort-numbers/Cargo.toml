[package]
name = "sort-numbers"
version = "0.1.0"
edition = "2021"

[dependencies]
vqsort-rs = "0.1.0"

[dev-dependencies]
rand = { version = "0.8.5", default-features = false, features = ["std"] }
rand_pcg = "0.3.1"
